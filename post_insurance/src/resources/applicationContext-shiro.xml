<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
	http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd"
	default-lazy-init="true">

	<description>Shiro安全配置</description>

	<!-- Shiro's main business-tier object for web-enabled applications -->
	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		<property name="realm" ref="shiroDbRealm" />
		<property name="cacheManager" ref="shiroEhcacheManager" />
	</bean>

	<!-- 項目自定义的Realm -->
	<bean id="shiroDbRealm" class="com.gdpost.web.shiro.ShiroDbRealm" depends-on="userDAO, userRoleDAO, organizationRoleDAO, roleDAO, MUserDAO, MUserRoleDAO, MRoleDAO">
		<property name="userService" ref="userServiceImpl"/>
		<property name="userRoleService" ref="userRoleServiceImpl"/>
		<property name="roleService" ref="roleServiceImpl"/>
		<property name="organizationRoleService" ref="organizationRoleServiceImpl"/>
		<property name="mUserService" ref="MUserServiceImpl"/>
		<property name="mUserRoleService" ref="MUserRoleServiceImpl"/>
		<!-- 是否使用验证码 -->
		<property name="useCaptcha" value="true"/>
		<!-- 是否激活超级用户，生成环境建议关闭该功能 -->
		<property name="activeRoot" value="true"/>
	</bean>

	<!-- Shiro Filter -->
	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<property name="securityManager" ref="securityManager" />
		<property name="loginUrl" value="/login" />
		<property name="successUrl" value="/web/index" />
        <property name="filters">
	        <map>
	        	<!-- 
	        	<entry key="authc" value-ref="baseFormAuthenticationFilter"/>
	        	-->
	        	<!-- 是否启用验证码检验 -->
	            <entry key="authc" value-ref="captchaFormAuthenticationFilter"/>
	            <entry key="user" value-ref="dWZUserFilter"/>
	        </map>
        </property>
		<property name="filterChainDefinitions">
			<value>
				/Captcha.jpg = anon
				/styles/** = anon
				/images/** = anon
				/css/** = anon
				/js/** = anon
				/login/timeout = anon
				/members/member/register = anon
				/web/index = anon
				/web/tologin = anon
				/login/manager = anon
				/login = authc
				/logout = logout
		    	/** = user
		 	</value>
		</property>
	</bean>
	
	<!-- 用户授权信息Cache, 采用EhCache -->
	<bean id="shiroEhcacheManager" class="org.apache.shiro.cache.ehcache.EhCacheManager">
		<property name="cacheManagerConfigFile" value="classpath:ehcache/ehcache-shiro.xml"/>
	</bean>
	
	<bean id="captchaFormAuthenticationFilter" class="com.gdpost.web.shiro.CaptchaFormAuthenticationFilter">
		<property name="defaultSuccessUrl" value="${shiro.default.success.url}"/>
        <property name="adminDefaultSuccessUrl" value="${shiro.admin.default.success.url}"/>
        <property name="webDefaultSuccessUrl" value="${shiro.web.default.success.url}"/>
	</bean>
	<bean id="dWZUserFilter" class="com.gdpost.web.shiro.DWZUserFilter"/>
	
	<!-- 保证实现了Shiro内部lifecycle函数的bean执行 -->
	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>
</beans>